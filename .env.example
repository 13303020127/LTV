# DecoTV - Cloudflare Pages 环境变量配置

# 存储配置
# 选项: 'localstorage' | 'redis' | 'upstash' | 'kvrocks'
NEXT_PUBLIC_STORAGE_TYPE=localstorage

# 用户注册
# 设置为'true'启用用户自助注册（需要redis/upstash/kvrocks存储）
NEXT_PUBLIC_ENABLE_REGISTRATION=false

# Redis/数据库连接信息（可选）
# REDIS_URL=redis://username:password@hostname:port
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Upstash Redis配置（使用'upstash'时 - 推荐用于Vercel/Cloudflare）
UPSTASH_REDIS_REST_URL=https://your-upstash-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Kvrocks配置（使用'kvrocks'时）
KVROCKS_URL=

# 管理员配置
# ADMIN_PASSWORD=your_secure_admin_password

# 可选：外部API密钥
DOUBAN_API_KEY=
TMDB_API_KEY=

# API代理配置（可选）
# API_PROXY_URL=https://your-proxy-domain.com

# 站点名称配置
# SITE_NAME=DecoTV

# 注意：部署到Cloudflare Pages时，需要在Cloudflare控制台设置以上环境变量
# 1. 登录Cloudflare Dashboard > Pages > 选择您的项目
# 2. 点击设置 > 环境变量
# 3. 添加上述必要的环境变量